# 后训练知识笔记: SFT + LoRA + 评测

> 配套本仓库实践的知识复盘: 底层机制、数学直觉、踩坑根因、评测设计。
> 所有内容都在本项目真实跑通过, 数字均为实测。

## 1. SFT 底层机制: 一条数据的完整旅程

### 1.1 五站旅程

```
train.jsonl (messages)
  → 拆分 prompt / completion        # 画出"题目/答案"分界线
  → chat template 渲染              # 对话结构序列化成带特殊 token 的文本
  → tokenize                        # 文本 → token id 序列
  → label mask                      # prompt 段 label=-100, completion 段 label=id
  → cross-entropy                   # loss 只落在 assistant 段
```

- **拆分的本质**: 监督学习只监督答案侧, 告诉框架分界线在哪, loss 才知道算在哪段。
- **chat template**: `<|im_start|>` / `<|im_end|>` 是词表"保留字"(id 151644/151645),
  普通文本的 BPE 切分不会产生它们; 模型在预训练时见过海量这种结构, 天然理解角色切换。
  **训练与推理必须用同一 template**, 否则分布偏移, 效果直接崩。
- **tokenize**: BPE 把高频字/词组合合并成一个 token, Qwen2.5 词表约 15 万,
  中文约 1~1.5 字/token。token id 是 embedding 矩阵的行号。

### 1.2 label mask: SFT 的灵魂

- `labels` 是 `input_ids` 的副本, prompt 区间全部改成 **-100**;
- -100 是 PyTorch `CrossEntropyLoss` 的默认 `ignore_index`——不进 loss、不回梯度;
- 类比: 整张卷子只有答案区给分, 题目写得再好也不计分。

### 1.3 shift: 模型永远在预测"下一个 token"

`logits[t]` 是对 token[t+1] 的分布, loss 内部对齐 `logits[:, :-1]` vs `labels[:, 1:]`。
所以 completion 的第一个 token 是由 prompt 最后一个 token 位置的输出预测的——
模型从 prompt 末尾就开始答题。"prompt 不算 loss"的精确含义:
**所有预测目标落在 prompt 区间内的预测位置被作废**。

### 1.4 预训练 vs SFT

| | 预训练 | SFT |
|---|---|---|
| loss 范围 | 全序列每个 token | 只有 assistant 段 |
| 学什么 | 语言统计规律 | 响应协议(被问 X 该按什么格式回 Y) |
| 数据规模 | 万亿 token 网页/代码 | 千~百万条 (指令, 回答) 对 |
| 类比 | 读书学会中文 | 岗前培训 |

隐含假设: 语言能力已在预训练中获得, SFT 是在能力大树上"嫁接"任务行为——
这就是 1200 条数据能教会一个模型的原因。

### 1.5 loss 曲线解读: 为什么能降到 0.0002

- 任务是**确定性映射**(一句话只有一个正确答案), loss 趋近 0 合理;
  反例: 开放域对话多种合理回答分散概率, loss 降到 1~2 就到头。
- 225 步 = 1200 样本 ÷ (batch 8 × 梯度累积 2) × 3 epochs;
- warmup 10 步: 初始 LoRA 权重随机、梯度噪声大, lr 从 0 爬升防开局震荡;
- **loss 极低 ≠ 过拟合**——held-out 句式不相交仍 98.5%, 证明学到的是泛化映射。
  过拟合的判定标准永远是 held-out 指标, 不是 train loss。

## 2. LoRA: 低秩假设的数学直觉

### 2.1 假设

微调的数学本质是求 `W = W₀ + ΔW`。LoRA(Microsoft 2021)主张:
**ΔW 的内在秩很低**——任务适配是定向修正, 不需要满秩自由度。
类比: 500MB 单体应用加一个接口, 不该 fork 整个仓库改每个文件。

`ΔW = B·A`(B: d×r, A: r×d): 896×896 的 802,816 自由度, r=16 时只剩 28,672(3.6%)。

### 2.2 前向公式与两个初始化细节

```
h = W₀x + (α/r) · B · A · x
```

- **B 全零 + A 高斯初始化**: 第 0 步 `B·A = 0` → ΔW=0 → 输出与 base 完全一致,
  从"无损起点"出发。若都随机初始化, 开局就破坏预训练分布。
- **α/r 缩放**(α=2r → 恒为 2.0): 解耦"秩大小"与"变化幅度", 调 r 不用重调 lr。

### 2.3 挂载层与参数账本

Qwen2.5-0.5B(hidden=896, 24 层, FFN 中间维 4864), r=16 每层:

| 层 | 形状 | LoRA 参数 |
|---|---|---|
| q_proj / o_proj | 896×896 | 28,672 ×2 |
| k_proj / v_proj | 896×128 (GQA: 14 个查询头共享 2 个 KV 头) | 16,384 ×2 |
| gate/up/down_proj | 896×4864 | 92,160 ×3 |
| 每层合计 | | 366,592 |

×24 层 = **8,798,208(1.75%)**, 与训练日志逐位一致。线性公式: `trainable = 549,888 × r`。
原论文只挂 q/v; 现代实践全挂 7 类——FFN 是知识容量大头。

### 2.4 学习率为什么敢大一个数量级

- 全量微调 1e-5~2e-5: 动的是预训练权重本身, 大步长 = 灾难性遗忘风险;
- LoRA 2e-4: 旁路是随机初始化的新参数, 无历史包袱可破坏; 小参数量梯度信噪比低,
  需要更大步长才能在有限步数内收敛;
- 类比: 改生产老代码要小步灰度, 新写的独立模块可以放手重构。

### 2.5 推理零开销与合并

`W' = W₀ + (α/r)BA` 可预计算后原地写回, 合并后计算图与原模型完全一致。
adapter 仅 ~35MB → "一份 base + N 个任务 adapter 热插拔"。
注意注入/合并都是**单向操作**(见坑 2)。

### 2.6 显存账本: 16 bytes/param 规则

AdamW 混合精度下, **每个可训练参数**:

```
2B bf16 权重 + 2B bf16 梯度 + 4B fp32 master + 4B 一阶动量 + 4B 二阶动量 = 16 bytes
```

**冻结参数只占 2 bytes**(只剩权重本体)。优化器状态(12/16)才是全量微调的大头:

| 场景 | 参数状态显存 |
|---|---|
| 0.5B 全量微调 | ≈ 8.0 GB |
| 0.5B + LoRA | ≈ 1.1 GB |
| 7B 全量微调 | ≈ 112 GB(80G A100 单卡都放不下) |
| 7B + LoRA | ≈ 14.5 GB(单张 24G 卡可跑) |

这就是工业界默认 PEFT 的根本原因。

## 3. 三个坑的根因深挖

### 3.1 `assistant_only_loss` 与 `{% generation %}` 标记

机制链: `assistant_only_loss=True` → TRL 调
`apply_chat_template(..., return_assistant_tokens_mask=True)` → 掩码生成依赖模板里的
`{% generation %}` 块(HF 对 Jinja2 的扩展) → Qwen2.5 没写(Qwen3 才加) → 掩码全 0 → 报错。

本质: **chat template 是三方元数据契约**(模型发布方/训练框架/推理框架共享),
契约缺字段, 框架的"自动"就失效。TRL 只对少数已知模型族自动 patch 模板。
解法: prompt-completion 格式, 分界线从"模板推断"变为"数据显式给定"。

### 3.2 PEFT 原地注入与对照组污染

`PeftModel.from_pretrained(base, path)` **原地**遍历 target_modules 替换 `nn.Linear`
→ base 和 lora 是两个名字、同一棵对象树 → `gen(base)` 走激活旁路的前向 →
对照组偷吃了试验药, A/B 对比无声失效。

解法: `with lora.disable_adapter():` 上下文内推理, 旁路被短路, 等价纯 base 前向。
教训: **对照组完整性是实验有效性的前提** + 警惕原地修改穿透所有 Python 引用。

### 3.3 评测驱动数据迭代

"音量设置成40"不含方向信息 → `P(direction|话语) = 0.5/0.5` → 最优模型也注定 50% 错,
F1 天花板被标注质量钉死, 与模型容量、超参无关。
修复: 槽位存在性由话语内容决定(提及才输出)——这同时修正了任务定义, 不只是数据。

闭环: **评测 → 错误分析 → 数据审计 → 修数据 → 重训 → 再评测**。

### 3.4 共同模式: 后端经验直接迁移

| 坑 | 深层根因 | 后端对应物 |
|---|---|---|
| 1 | 元数据契约缺字段 | API schema 演进/兼容性 |
| 2 | 共享对象被原地修改 | 全局单例/可变状态副作用穿透引用 |
| 3 | 上游数据质量 | ETL 数据审计, garbage in garbage out |

ML 工程的调试功力一大半不是 ML 知识, 而是契约意识、引用/副作用意识、数据质量意识。

## 4. 评测体系设计

### 4.1 三层指标: 递进的"能解析 → 懂语义 → 抽得准"

- **格式合法率**: 系统可用性底线(下游 parse 失败 = 链路故障), SFT 最立竿见影;
- **意图准确率**: 业务主指标;
- **槽位 P/R/F1**: 结构化抽取质量。

分层才有诊断价值: L1 崩 = 训练/模板问题; L2 崩 = 协议没学会; L3 低 = 槽位定义/标注问题。
本项目最有价值的对照: **base 格式合法率 97% vs 意图准确率 0%**——
base 会 JSON 的"形", 不懂标签协议的"义"。**SFT 教的不是"输出 JSON",
而是"输出这套协议的 JSON"**。

### 4.2 held-out: 句式池不相交

数据是"模板×词表"程序化生成的, 随机划分会让同一模板的不同填充横跨 train/eval,
模型背模板就能拿高分(指标虚高)。句式不相交后, eval 的每句话**说法**模型没见过,
**词表元素**都见过——考的是"换个说法还能不能懂"(组合泛化)。
工业界对应: 按用户/时间切分防数据泄漏。

### 4.3 槽位 micro-F1

slots 拆成 `(key, value)` 对做集合运算: 交集 = TP, pred 多出 = FP(多说/说错),
GT 未覆盖 = FN(漏说)。

```
P = TP/(TP+FP)   # 惩罚多说
R = TP/(TP+FN)   # 惩罚漏说
F1 = 2PR/(P+R)
```

- 不用 accuracy: 抽取任务负类(未提及槽位)占绝大多数, 全空预测 acc 也很高;
- micro(跨样本累加再算)让大类意图主导; macro 会被小类放大, NLU 场景选 micro;
- 值是**字符串级精确匹配**: `08:00 ≠ 8点`, 考规范化映射而非复制。

### 4.4 模型输出 = 不可信外部输入

`extract_json`: 正则截取 JSON 块(模型可能带 markdown 围栏) + schema 校验
(`intent` 是 str、`slots` 是 dict——base 真的会输出 list)。
生产系统里 LLM 输出永远要过"解析 + 校验 + 兜底"三层, 和调第三方 API 一个待遇。

### 4.5 回归测试与灾难性遗忘

SFT 在窄分布上训练可能挤压通用能力。5 道回归题覆盖自我认知/事实/翻译/推理/代码。
**LoRA 天然比全量微调更不易遗忘**: 低秩约束限制了权重能偏多远, 本身是一种正则化。
工业做法: lm-evaluation-harness 跑 MMLU/C-Eval 子集, 微调前后对比, 掉分设阈值告警。

### 4.6 评测要确定性

`do_sample=False` 贪心解码: 同一模型跑两次逐字节一致,
才能区分"指标变化"和"采样噪声"。
